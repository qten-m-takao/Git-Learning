version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name work-kadai-5 --s3-location s3://work-kadai/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src